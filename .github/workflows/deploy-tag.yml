name: Release
concurrency: release

on:
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
    - name: Inject slug/short variables
      uses: rlespinasse/github-slug-action@v3.x

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v1
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Get Major Version
      id: version
      uses: rishabhgupta/split-by@v1
      with:
        string: '${{ env.GITHUB_REF_SLUG }}'
        split-by: '.'

    - name: Print Major Version
      run: | 
        echo "Major Version: ${{ steps.version.outputs._0 }} for ${{ env.GITHUB_REF_SLUG }}"

    - name: Cache Docker layers
      uses: actions/cache@v2
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-buildx-
    
    - name: Login to Docker
      uses: docker/login-action@v1 
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: BUILD
      id: docker_build
      uses: docker/build-push-action@v2
      with:
        push: false
        load: true
        cache-from: type=local,src=/tmp/.buildx-cache
        cache-to: type=local,dest=/tmp/.buildx-cache-new
        tags: resizer-temp

    - name: Move cache
      run: |
        rm -rf /tmp/.buildx-cache
        mv /tmp/.buildx-cache-new /tmp/.buildx-cache

    - name: Rename Tags
      run: |
        docker tag resizer-temp squidex/resizer:latest
        docker tag resizer-temp squidex/resizer:${{ env.GITHUB_REF_SLUG }}
        docker tag resizer-temp squidex/resizer:${{ steps.version.outputs._0 }}

    - name: Push Tags
      run: |
        docker push squidex/resizer:latest
        docker push squidex/resizer:${{ env.GITHUB_REF_SLUG }}
        docker push squidex/resizer:${{ steps.version.outputs._0 }}


